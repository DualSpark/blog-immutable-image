---
- hosts: all
  vars:
    http_port: 8080
  remote_user: root
  tasks:
  - name: set environment variables
    lineinfile:
      dest=/home/ec2-user/goapp
      regexp="GOAPP_HTTPPORT=\"\""
      line="\texport GOAPP_HTTPPORT=\"8080\""
